<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunos.tv.appupgrade.dal.dao.TestGroupDeviceDAO">

	<!--获取一个设备所有的有效测试分组,取最新最多只取200个 test_group_device大表，test_group小表  -->	
	<select id="getListByUuid" resultType="TestGroupDeviceDo" parameterType="String">
		select d.`group_id`
		FROM `test_group_device` d, `test_group` g
		WHERE
			 d.`uuid`=#{uuid} AND g.id=d.group_id AND d.`is_delete` = 0 AND g.`is_delete` = 0
		ORDER BY d.`id` DESC	
		LIMIT 200
	</select>

</mapper>